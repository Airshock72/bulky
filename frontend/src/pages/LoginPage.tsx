import LoginForm from '@/components/LoginForm'

const LoginPage = () => (
  <section className='flex min-h-[calc(100vh-4rem)] items-center justify-center px-4 py-12 sm:px-6 animate-fade-in-up'>
    <LoginForm />
  </section>
)

export default LoginPage
